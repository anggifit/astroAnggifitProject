---
import { Image } from "astro:assets";

const { src, alt, width } = Astro.props;
---

<Image
  src={src}
  alt={alt}
  class={`w-[${width}] h-[27.5rem] rounded-tr-[60px] rounded-bl-[60px] shadow-lg relative z-20 object-cover`}
/>
